# پُستیار — مشخصات محصول (Product Specification)

نسخه: 1.0.0 | وضعیت: منجمد (فاز ۰۶ قرارداد اجرا)

## ۱. تعریف محصول

پُستیار یک پلتفرم SaaS مستقل برای مدیریت محتوا، کانال‌ها، ربات‌ها، اتوماسیون، هوش مصنوعی، یکپارچگی ووکامرس، انتشار نرخ طلا، اشتراک، اعلان‌ها و تحلیل آمار روی تلگرام، بله و روبیکا است.

```
یک‌بار بساز → مقصدها را انتخاب کن → الان یا زمان‌بندی‌شده منتشر کن → همهٔ نتایج را ردیابی کن
```

## ۲. منشأ و مراجع

- `audits/asovin/AUDIT-BACKEND.md` — رفتار محصول تاریخی (WHCM SaaS): ۳۳ جدول، ۶۱ اندپوینت API موبایل، قوانین کسب‌وکار.
- `audits/asovin/AUDIT-FRONTEND-UX.md` — معماری اطلاعات داشبورد/ادمین، واژه‌نامهٔ فارسی، تقویم جلالی.
- `audits/woo-hooman-channel-manager/AUDIT.md` — رفتار تحویل تلگرام/بله، اتصال ووکامرس، ماژول طلا.
- `audits/assets/ASSET-INVENTORY.md` — هویت بصری رسمی (لوگو، آیکون‌ها، وزیرمتن).

رفتار مرجع فقط جایی پذیرفته می‌شود که با معماری جدید تناقض نداشته باشد.

## ۳. تأمین‌کنندگان کانال (درجه اول)

| تأمین‌کننده | API | قابلیت‌های اعلام‌شده |
|---|---|---|
| تلگرام | `api.telegram.org/bot<TOKEN>` | متن، رسانه، ویرایش، حذف، دکمهٔ شیشه‌ای (inline)، reply keyboard، webhook با secret_token، getMe، getUpdates |
| بله | `tapi.bale.ai/bot<TOKEN>` (هم‌خانوادهٔ Bot API تلگرام) | متن، رسانه، ویرایش، دکمه، webhook، getMe، getUpdates (حذف پیام: عدم پشتیبانی اعلام می‌شود) |
| روبیکا | `botapi.rubika.ir` (Bot API رسمی روبیکا) | متن، رسانه، ویرایش، getMe، webhook/بررسی سلامت؛ قابلیت‌های نامطمئن به‌صورت «پشتیبانی‌نشده» اعلام می‌شوند |

قانون: هیچ قابلیتی شبیه‌سازی نمی‌شود. هر تأمین‌کننده نقشهٔ قابلیت صریح دارد (`ProviderCapabilities`) و رابط کاربری عملیات پشتیبانی‌نشده را غیرفعال/توضیح‌دار نشان می‌دهد.

## ۴. دامنهٔ محصول

### ۴.۱ احراز هویت و کاربران
- ثبت‌نام: نام، نام خانوادگی، موبایل، ایمیل، نام کسب‌وکار، نوع فعالیت، رمز عبور، تکرار رمز، پذیرش قوانین.
- ورود/خروج، چرخش نشست، ابطال نشست، بازیابی رمز (توکن کوتاه‌عمر یک‌بارمصرف)، Argon2id، CSRF، محدودسازی نرخ.
- اولین ثبت‌نام (تعداد کاربر = ۰) → `SUPER_ADMIN` به‌صورت تراکنشی؛ بقیه → `USER`.

### ۴.۲ کانال‌ها
اتصال/تأیید/ویرایش/قطع کانال تلگرام، بله، روبیکا؛ ذخیرهٔ رمز با AES-256-GCM؛ بررسی سلامت؛ مالکیت تک‌مستأجری؛ ثبت ضدتقلب (پلتفرم+شناسهٔ کانال فقط یک‌بار قابل ثبت است).

### ۴.۳ انتشار و زمان‌بندی
- ایجاد پست (متن + رسانه + دکمه‌ها + لینک‌ها) → انتخاب مقصدها → انتشار فوری یا زمان‌بندی (تقویم جلالی).
- Outbox + صف + Worker + ثبت Delivery با وضعیت: `PENDING/PROCESSING/SENT/RETRYING/FAILED/CANCELLED` (برچسب فارسی: در انتظار، در حال پردازش، ارسال شد، در حال تلاش مجدد، ناموفق، لغو شد).
- Idempotency کامل، retry طبقه‌بندی‌شدهٔ محدود، نمایش واقعی شکست‌ها به کاربر.

### ۴.۴ ربات‌ها (دامنهٔ درجه اول)
اتصال با توکن رسمی (تأمین بیرونی مثل BotFather با راهنمای درون‌داشبوردی)، تأیید، فعال/غیرفعال، دستورها، پاسخ‌ها، دکمه‌ها، گردش‌کار، رفتار هوش مصنوعی، رویدادهای ورودی (webhook/getUpdates)، کاربران ربات، تحلیل ربات، سلامت و خطاها.

### ۴.۵ سازندهٔ ربات (بدون کد)
Trigger → Condition/Step → Action → ... با اکشن‌های پایه: ارسال پیام، ارسال دکمه، پاسخ هوش مصنوعی، توقف (توقف/تأخیر). محدودیت‌های ایمنی: حداکثر گام، مدت اجرا، تودرتویی، تکرار، فراخوانی AI، خروجی در هر اجرا.

### ۴.۶ هوش مصنوعی
انتزاع چندتأمین‌کننده (OpenAI، Gemini، DeepSeek، Claude، OpenRouter، Mistral، سفارشی)، اجرای سنگین از طریق صف، محاسبهٔ مصرف، محافظت از تزریق پرامپت، کلیدها فقط سمت سرور، کپشن‌ساز و پاسخگوی هوشمند.

### ۴.۷ ووکامرس (Postyar Connector)
افزونهٔ رسمی وردپرس «Postyar Connector»: اتصال امن سایت، همگام‌سازی محصول، انتشار محصول، نگاشت دسته، webhook با امضای HMAC، تلاش مجدد، گزارش وضعیت، عیب‌یابی. سمت SaaS: API یکپارچگی با احراز site-key/secret.

### ۴.۸ ماژول طلا
نمایش نرخ لحظه‌ای، منبع پیکربندی‌پذیر، به‌روزرسانی زمان‌بندی‌شده با تشخیص تغییر، انتخاب کانال، قالب/فرمت، منطقهٔ زمانی، رابط جلالی؛ استفاده از همان صف/تحویل/idempotency.

### ۴.۹ اشتراک، پرداخت، کیف پول، زیرمجموعه‌گیری
- پلن‌ها: رایگان، پایه، حرفه‌ای، تجاری، سازمانی. حدود: کانال، پست، AI، ربات، زمان‌بندی، فضا — اعمال سمت سرور.
- درگاه پرداخت با انتزاع (زرین‌پال به‌عنوان آداپتور مرجع)، تأیید سمت سرور، idempotency کامل.
- کیف پول دفترکل‌محور: `CREDIT/DEBIT/REFUND/BONUS/PAYMENT/ADJUSTMENT`، تراکنش امن، مبالغ صحیح (Rial، BIGINT).
- زیرمجموعه‌گیری: کد معرف، پاداش ثبت‌نام + پاداش اولین خرید، محاسبهٔ idempotent، اتصال به دفترکل.

### ۴.۱۰ اعلان‌ها
درون‌برنامه‌ای + تلگرام/بله/روبیکا + ایمیل/پیامک (در صورت پیکربندی). هشدار انقضای اشتراک دقیقاً ۷ روز قبل، یک‌بار برای هر اشتراک با قاعدهٔ یکتایی ماندگار.

### ۴.۱۱ تحلیل و رویداد
مجموعهٔ رویداد تعریف‌شده در §۲۴ قرارداد، جدول رویداد + تجمیع روزانهٔ read-model، گزارش‌های حساب/کانال/ربات/انتشار/گردش‌کار/AI/مالی، خط زمانی فعالیت، خلاصهٔ خطاها — همهٔ tenant-scoped.

### ۴.۱۲ ادمین و پشتیبانی
پنل ادمین permission-aware با audit: کاربران، پلن‌ها، اشتراک‌ها، پرداخت‌ها، کدهای تخفیف، تنظیمات سراسری AI/SMS/ایمیل/طلا، اعلان همگانی، تیکت‌ها. پشتیبانی تیکت‌محور با مالکیت سختگیرانهٔ مستأجری.

### ۴.۱۳ رسانه
ذخیرهٔ خصوصی خارج از web root، متادیتا در DB، دسترسی کنترل‌شده، اعتبارسنجی اندازه/MIME/امضای فایل/بُعد، تبدیل WebP محدودحافظه.

## ۵. قراردادهای زبان، عدد، تاریخ، برند

- تمام سطوح کاربر فارسی؛ بدون هیچ واژهٔ انگلیسی قابل‌مشاهده.
- ارقام فارسی در همهٔ خروجی‌های عددی؛ ابزارهای مشترک `faDigits/toJalali/formatMoney/...`.
- تاریخ جلالی در همهٔ نمایش‌ها؛ ذخیره‌سازی UTC، نمایش Asia/Tehran.
- فونت وزیرمتن (assets/brand/fonts)، لوگوی رسمی (logo-white-bg برای پس‌زمینهٔ تیره، logo-full برای هیرو)، رنگ برند مرجع `#6366f1`.
- RTL از پایه؛ واژه‌نامهٔ یکدست از AUDIT-FRONTEND-UX.md (کانال، ربات، انتشار، زمان‌بندی، کیف پول، اشتراک، زیرمجموعه‌گیری، ربات نرخ لحظه‌ای طلا و سکه، تیکت، ...).

## ۶. قوانین کسب‌وکار منتخب از مراجع (تصحیح‌شده)

| موضوع | تصمیم جدید |
|---|---|
| پلن رایگان | ۲ کانال (ترکیبی از هر سه پلتفرم)، ۱۰ پست در دوره، بدون ربات AI |
| `max_posts = 0` | نامحدود (مانند مرجع) |
| سهمیهٔ پست | شمارش در دورهٔ اشتراک جاری |
| یادآور انقضا | **۷ روز قبل** (قرارداد master؛ مرجع ۳ روز بود)، یک‌بارمصرف |
| پاداش معرفی | ثبت‌نام: ۱۰۰ امتیاز؛ اولین پرداخت: ۱۰٪ مبلغ (اصلاحِ باگِ اجرا نشدن مرجع)، سقف ماهانه قابل‌پیکربندی |
| امتیاز → تومان | ۱ امتیاز = ۱ تومان (یکسان‌سازی وب/API) |
| ثبت ضدتقلب کانال | (platform, channel_id) فقط یک‌بار به اولین مالک — با امکان آزادسازی توسط ادمین |
| پیامک | حداکثر ۳ پیام/ساعت/شماره |
| حذف تخفیف | شمارندهٔ استفاده تراکنشی (اصلاح باگ مرجع) |

## ۷. خارج از دامنهٔ نسخهٔ ۱
پرداخت ارزی، چندمستأجری تیمی/سازمانی با عضویت، اپ موبایل نیتیو، فروش مستقیم محصولات ووکامرس در SaaS، درگاه‌های پرداخت متعدد (فقط انتزاع + یک آداپتور مرجع).
